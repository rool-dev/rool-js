# rool-cli

Command-line interface for the Rool platform. Published as `@rool-dev/cli`.

## Commands
- `pnpm build` - Compile TypeScript
- `pnpm typecheck` - Type check without emitting

See also the @packages/cli/README.md

## Local Development
```bash
pnpm install
pnpm build
pnpm link --global
```
