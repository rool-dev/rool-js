import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.rool.example.auth',
  appName: 'Rool Auth Example',
  webDir: 'dist',
};

export default config;
